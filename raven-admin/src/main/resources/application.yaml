spring:
  application:
    name: raven-admin
  datasource:
      url: jdbc:mysql://localhost:3306/imdb?useUnicode=true&characterEncoding=UTF-8&useSSL=false
      username: root
      password: 123456
      driver-class-name: com.mysql.jdbc.Driver

nacos:
  config:
    server-addr: localhost:2181

server:
  port: 8888

